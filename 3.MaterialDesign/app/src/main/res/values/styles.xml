<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Style applied to all CardViews -->
    <style name="CardViewStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_marginRight">10dp</item>
    </style>

    <!-- Style applied to the last CardView -->
    <style name="CardViewStyle.LastCard">
        <item name="android:layout_marginBottom">10dp</item>
    </style>

    <!-- Style applied to all texts displaying the date -->
    <style name="DateText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">20dp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Style applied to al texts displaying the reminder -->
    <style name="DateText.ReminderText">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:ems">15</item>
        <item name="android:textStyle">normal</item>
    </style>

    <!-- Style applied to the floating button-->
    <style name ="FloatingButtonStyle">
        <item name="android:layout_width" >wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">end|bottom</item>
        <item name="android:layout_margin">20dp</item>
        <item name="android:backgroundTint">@color/GreenRadioButton</item>
        <item name="backgroundTint">@color/GreenRadioButton</item>
    </style>

    <!-- Style applied to RadioButton -->
    <style name="RadioButtonStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_alignParentEnd">true</item>
        <item name="android:layoutDirection">rtl</item>
        <item name="android:buttonTint">@color/GreenRadioButton</item>
    </style>

    <!-- Style applied to the buttons on disabled Card Views-->
    <style name="RadioButtonStyle.CheckedOnes">
        <item name="android:checked">true</item>
        <item name="android:alpha">0.3</item>
    </style>

    <!-- Style applied to the Relative Layout inside Card Views -->
    <style name="RelativeLayoutCardView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="ToolBarStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@color/GreenRadioButton</item>
    </style>

</resources>
